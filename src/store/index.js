import { createStore } from "vuex";
/* Modules */
import text from "./modules/text.module";

const state = {
};

const getters = {};

const mutations = {};

const actions = {};

export default createStore({
  state,
  getters,
  mutations,
  actions,
  modules: {
    text,
  },
});
